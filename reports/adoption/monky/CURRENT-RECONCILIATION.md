# Ermine → Monky — current reconciliation

This report reconciles the frozen U2 adoption ledger with the current Monky code after
the U8/U8e migration, the theme-plane binding, and the live style cleanup.

It does **not** rewrite `ledger.json`. The ledger is conserved historical evidence,
measured at Monky `bd4bc41`. This report answers the different question: what does that
old accounting mean when compared with current Monky `9a6ff91`?

## Headline

The old `skin-local = 339` number is not the current Monky state.

Of the 339 historical `skin-local` rows:

| Current check | Rows | Meaning |
|---|---:|---|
| Source file no longer exists | 131 | Retired by migration/dead-style cleanup |
| File exists, exact declaration no longer appears | 92 | Superseded, rewritten, or absorbed by later Ermine work |
| Exact declaration still appears | 116 | Still needs current classification |

So at least **223 / 339** historical `skin-local` rows are stale or superseded as literal
current-code evidence. The remaining **116** are real current declarations, but they are not
automatically failures: some are now backed by Ermine skin/theme vocabulary, some are
project identity, and some are still design-evolution evidence.

## Why the number appeared stuck

`COVERAGE.md` was reading two different layers as if they were one:

1. **Frozen conserved declaration ledger** — a U2 baseline. It intentionally preserves old
   rows even when later migrations delete, rewrite, or absorb the source.
2. **Current Monky implementation** — the live repository after U5–U8e and the style audit.

The frozen ledger still contains evidence strings such as "No skin vocabulary is ruled in
Ermine yet." That was true when those rows were classified. It is no longer true after the
skin colour plane, role composition, rule/corner/type carriers, and Monky theme binding.

## Current live style audit

Monky's current build-backed audit, generated by `npm run audit:styles`, reports:

| Metric | Value |
|---|---:|
| Defined classes | 198 |
| Selector blocks | 288 |
| Live static classes | 198 |
| Dead-candidate declarations | 0 |

This means Monky no longer has a known dead selector inventory in the live CSS. The old
ledger's `dead` and `skin-local` rows are still useful as migration history, but they are
not current reachability facts.

## Ledger rows checked against current Monky

The check below is intentionally conservative: it compares each old ledger row against the
current file path and literal `property: value` text. It is not a new terminal ledger, and
it does not prove selector-level equivalence. It is good enough to identify stale baseline
rows that should stop being read as current state.

| Frozen disposition | Total | File missing | Declaration changed/absent | Literal declaration present |
|---|---:|---:|---:|---:|
| identity-local | 138 | 1 | 5 | 132 |
| skin-local | 339 | 131 | 92 | 116 |
| grammar-composition | 46 | 0 | 39 | 7 |
| grammar-exact | 104 | 39 | 63 | 2 |
| dead | 416 | 348 | 63 | 5 |
| substrate | 37 | 35 | 0 | 2 |
| uncertain | 329 | 259 | 70 | 0 |

The important asymmetry is expected: once a declaration is migrated into Ermine words, the
old literal declaration usually disappears from the component sheet. That makes the
historical `grammar-*` and `skin-local` rows look "unchanged" in the frozen table, even
though the live source has moved.

## Historical `skin-local` rows by source file

| Frozen source file | Total | File missing | Declaration changed/absent | Literal declaration present |
|---|---:|---:|---:|---:|
| `src/content/overlays/deleteConfirm/deleteConfirmStyles.ts` | 9 | 0 | 9 | 0 |
| `src/content/overlays/modal/modalStyles.css` | 16 | 0 | 1 | 15 |
| `src/content/overlays/suggestionsOverlay/suggestionsOverlayStyles.css` | 36 | 0 | 26 | 10 |
| `src/content/overlays/views/macroEditor/editorViewStyles.css` | 51 | 0 | 19 | 32 |
| `src/content/overlays/views/search/searchViewStyles.css` | 62 | 0 | 27 | 35 |
| `src/content/overlays/views/settings/settingsViewStyles.css` | 34 | 0 | 10 | 24 |
| `src/styles/layout-semantic.css` | 107 | 107 | 0 | 0 |
| `src/styles/utilities.css` | 24 | 24 | 0 | 0 |

The biggest stale block is decisive: the old `layout-semantic.css` and `utilities.css`
sources do not exist in current Monky.

## Remaining literal `skin-local` property families

The 116 current literal matches are concentrated here:

| Property family | Rows |
|---|---:|
| `color` | 21 |
| `background` | 14 |
| `background-color` | 13 |
| `transition` | 11 |
| `border` | 8 |
| `opacity` | 8 |
| `border-radius` | 5 |
| `box-shadow` | 5 |
| `font-family` | 4 |
| `text-align` | 3 |
| `border-bottom` | 2 |
| `border-bottom-color` | 2 |
| `line-height` | 2 |
| `border-bottom-left-radius` | 2 |
| `border-bottom-right-radius` | 2 |
| `font-style` | 2 |
| `border-right` | 2 |
| other one-off families | 9 |

These should be classified under the newer model, not under the old "No skin vocabulary is
ruled yet" evidence.

Expected resolution:

- **Theme-backed skin:** `color`, `background`, `background-color`, many `border-color`
  and status/surface declarations can now point to Ermine skin sockets/carriers.
- **Metric-backed skin:** some `border-radius`, `font-size`, `font-weight`, and line-height
  cases may be Ermine type/corner carriers or Monky metric configuration.
- **Project identity:** exact modal geometry, component-specific emphasis, special wordmark
  behavior, and editor affordance details stay Monky-owned.
- **Still-open evolution:** transition timing, opacity state treatment, scrollbar
  prominence, and elevation geometry belong to the named follow-up questions rather than
  ad hoc reclassification.

## Consequence for `COVERAGE.md`

The frozen declaration table should stay visible, because it is the conserved baseline. But
it must be read with the current reconciliation:

- `skin-local = 339` is a historical baseline bucket, not a live migration score.
- current Monky style reachability is clean: zero dead-candidate declarations.
- a future current-ledger generator should replace literal matching with selector-aware
  computed-style reconciliation against emitted Ermine CSS.

**Resolved (U8f).** That generator now exists — `adoption/current-ledger.ts`
(`npm run adoption:current`) writes `CURRENT-LEDGER.md` / `current-ledger.json`, a
selector-aware, reason-coded reconciliation of live Monky CSS against emitted Ermine
CSS — and the frozen ledger itself is terminal: zero uncertain records remain
(`pilots/LEDGER-CLOSURE.md`). This report stays as the reasoning bridge between the two
accountings.

## Follow-up: color/background assimilation

The first targeted reconciliation pass is recorded in
`pilots/COLOR-BACKGROUND-ASSIMILATION.md`. It focused on the historical `color`,
`background`, and `background-color` skin-local families.

Important correction: the earlier property-family counts were historical and file-level.
A selector-level scan of current Monky CSS found 23 actual current declarations in those
families before the pass. The pass assimilated the safe carrier cases into Ermine words and
left 11 local declarations, all tied to `aria-current`, parent-relational state,
pseudo-elements, transparent reset semantics, or button recipe mechanics.

## Follow-up: border/rule assimilation

The next targeted pass is recorded in `pilots/BORDER-RULE-ASSIMILATION.md`. It focused on
safe border-colour cases that Ermine can express as `rule` while component CSS keeps line
mechanics.

The pass converted modal dialog, search input-container, and segmented option separator
colours to `rule`. In the current adoption surfaces, border-family declarations dropped
from 16 to 13. The remaining cases are resets/absence, transparent spacing sentinels,
`aria-current`, focus, pseudo-element geometry, and segmented-control overlap mechanics.
